# API endpoint (unlikely but it may change)
# API_ENDPOINT: ""
# City, defaults to "ბათუმი" (Batumi)
# See API endpoint or website for available city names in Georgian
# CITY: ""

FIRESTORE_COLLECTION_ID: "processed_tasks"
FIRESTORE_DATABASE_ID: ""
# Doc path for the seen-tasks index (1 read/run instead of 1 read/task)
# FIRESTORE_SEEN_INDEX: "_meta/seen_task_ids"
# Set to "true" to arm one-off index backfill, then request ?backfill=true once
# BACKFILL_INDEX: "false"

GEMINI_API_KEY: ""
# defaults to gemini-2.5-flash
# GEMINI_MODEL: ""

TELEGRAM_BOT_TOKEN: ""
TELEGRAM_CHAT_ID: ""